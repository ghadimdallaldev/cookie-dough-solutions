# Cookie Dough Solutions — Marketing Site

Portfolio site for **Cookie Dough Solutions** (F&amp;B software studio) with a dedicated **Supplify** product page at `/supplify`.

This project lives **outside** the supplify_erp monorepo.

## Run locally

```bash
cd cookie-dough-solutions
npm install
npm run dev
```

Opens at http://localhost:5180 — **Home** `/` and **Supplify** `/supplify`.

## Build

```bash
npm run build
npm run preview
```

## Supplify screenshots

App mockups used on `/supplify` live in `public/images/supplify/` (see `MockupStage.tsx` for filenames). Additional raw captures are in `public/screenshots/`.

## Higgsfield hero scenes

See [docs/HIGGSFIELD_SETUP.md](./docs/HIGGSFIELD_SETUP.md) and [public/images/README.md](./public/images/README.md). Until `hero-cookie-dough.jpg` and `hero-supplify.jpg` exist, heroes use editorial fallbacks.

## Brand

- **Parent:** Cookie Dough Solutions — `hello@cookiedough.app`
- **Product:** Supplify — restaurant &amp; supplier ordering

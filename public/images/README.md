# Site images

Assets used by the live app only:

| Folder | Used on |
|--------|---------|
| `cookie-dough-homepage/` | Home `/` — see `src/data/cookie-dough-homepage.ts` |
| `supplify-cursor-pack/` | Supplify `/supplify` — see `src/data/supplify-cursor-pack.ts` |
| `al-maalem/` | Hero & lifestyle shots on `/ordering` — see `src/data/ordering-app-pack.ts` |
| `ordering-app-ui/` | Real ordering UI screenshots (menu, customize, cart, checkout) — see `ORDERING_APP_UI` in `ordering-app-pack.ts` |

Brand marks live in `public/brand/`.
